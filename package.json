{
  "name": "vidyashaale",
  "version": "1.0.0",
  "description": "Online education platform for scheduling classes and sharing learning materials",
  "private": true,
  "workspaces": [
    "services/*",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:infra && dotenv -e .env -- concurrently -k -n gateway,auth,class,meeting,resource,notification,frontend \"npm run dev -w services/gateway\" \"npm run dev -w services/auth\" \"npm run dev -w services/class\" \"npm run dev -w services/meeting\" \"npm run dev -w services/resource\" \"npm run dev -w services/notification\" \"npm run dev -w frontend\"",
    "dev:services": "dotenv -e .env -- concurrently -k -n gateway,auth,class,meeting,resource,notification \"npm run dev -w services/gateway\" \"npm run dev -w services/auth\" \"npm run dev -w services/class\" \"npm run dev -w services/meeting\" \"npm run dev -w services/resource\" \"npm run dev -w services/notification\"",
    "dev:frontend": "dotenv -e .env -- npm run dev -w frontend",
    "dev:infra": "docker compose up -d postgres redis",
    "dev:infra:down": "docker compose down",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "db:migrate": "dotenv -e .env -- bash -c 'psql \"$DATABASE_URL\" -f database/init.sql'",
    "docker:up": "docker compose --profile containerized up -d",
    "docker:down": "docker compose --profile containerized down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "concurrently": "^8.2.0",
    "dotenv-cli": "^7.4.2",
    "eslint": "^8.45.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-react": "^7.33.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "prettier": "^3.0.0",
    "typescript": "^5.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
